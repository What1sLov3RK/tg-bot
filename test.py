from bs4 import BeautifulSoup as BS
from pytube import YouTube
import requests
import os
import re
from config import API_KEY, PATH, VOICE_ROOT

file_name = '🔥досмотри до конца и выбери✅ shorts майнкрафт бравлстарс а4 чтовыбрать fyr funny minecraft'
print(os.path.join(VOICE_ROOT, file_name), 'rb')
